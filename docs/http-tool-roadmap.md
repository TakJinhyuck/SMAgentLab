# HTTP Tool 기능 로드맵

> 외부 API를 LLM 컨텍스트에 동적으로 연결하는 도구 시스템

## Phase 1 — MVP (현재 진행)

### 완료 항목
- [ ] DB 테이블 `ops_http_tool` + 마이그레이션
- [ ] Backend CRUD API (`/api/http-tools`)
- [ ] LLM 자동완성 등록 API (자연어 → 구조화된 도구 정의)
- [ ] HttpToolAgent 구현 (도구 선택 → 파라미터 검증 → 승인 → HTTP 호출 → 응답 삽입)
- [ ] Frontend 어드민 UI (도구 등록/수정/삭제/on·off 토글)
- [ ] Frontend 채팅 UI (도구 승인/거절/파라미터 입력)
- [ ] 도구별 타임아웃 설정 (기본 10초)
- [ ] 응답 크기 제한 (`max_response_kb`, 기본 50KB)

### 핵심 플로우

**어드민 (도구 등록)**
1. 관리자가 자연어로 API 정보 입력 (URL, 파라미터, 응답 예시)
2. LLM이 구조화된 도구 정의로 자동완성
3. 관리자 확인/수정 후 저장
4. 도구 목록에서 on/off 토글

**채팅 (도구 사용)**
1. 사용자 질문 → LLM이 활성 도구 중 매칭 판단
2. 파라미터 검증 → 누락 시 재질의
3. 전체 도구 목록 + 선택된 도구/파라미터 노출 → 승인/거절
4. 승인 시 HTTP 호출 → 응답을 LLM 컨텍스트에 삽입 → 최종 답변

## Phase 2 — 안정화

- [ ] 호출 이력 테이블 (audit log) — 누가, 언제, 어떤 도구, 어떤 파라미터
- [ ] 인증 토큰 암호화 저장 (Fernet)
- [ ] HTTP 에러 시 사용자 안내 메시지 (401/403/500/timeout 분류)
- [ ] 캐싱 — 동일 파라미터 N분 내 재호출 방지 (외부 API rate limit 보호)

## Phase 3 — 확장

- [ ] 도구 체이닝 (A 응답 → B 입력)
- [ ] MCP 서버 연동 전환
- [ ] 도구 버전 관리 (API 스펙 변경 대응)
- [ ] Sandbox/dry-run 모드 (실제 호출 없이 테스트)
